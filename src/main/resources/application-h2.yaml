spring:
  sql:
    init:
      mode: always
      data-locations: classpath:data-h2.sql
  datasource:
    url: jdbc:h2:mem:budgetbuddy;DATABASE_TO_UPPER=false
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  docker:
    compose:
      enabled: false
